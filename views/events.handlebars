<p> GLHF Events</p><br>

<ul>
	{{#each tours}}
		{{! I'm in a new block...and the context has changed }}
		<li>
			{{name}} - {{price}}
			{{if ../currencies}}
				({{../../currency.abbrev}})
			{{/if}}
		</li>
	{{/each}}
</ul>
{{#unless currencies}}
	<p>All dates in {{currency.name}}.</p>
{{/unless}}
{{#if specialsUrl}}
	{{! I'm in a new block..but the context hasn't changed (sortof) }}
	<p> Check out our <a href='{{specialsUrl}}'>dates!</p>
{{else}}
	<p>Please check back often for specials.</p>
{{/if}}
<p>
	{{#each currencies}}
		<a href='#' class='currency'>{{.}}</a>
	{{else}}
		Unfortunately, we currently only accept {{currecy.name}}
	{{/each}}
</p>
